// Simplified database interface for sql.js compatibility
import { all, get, run, initDB as initDatabase, saveDB, getDatabase } from "./schema.js";

export { all, get, run, initDatabase as initDB, saveDB, getDatabase };
